import Quickshell

SystemInfoModule {
    // Swap usage as a fraction
    property real swapUsage
    // Swap usage as a human-readable fraction with unit
    property string swapText
    // Memory usage as a fraction
    property real memUsage
    // Memory usage as a human-readable fraction with unit
    property string memText
}